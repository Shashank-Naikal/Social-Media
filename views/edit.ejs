<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 text-center">
      <div class="flex place-content-end -mt-4 mb-10">
        <a class="bg-red-800 px-3 py-1 rounded-md" href="/logout">LogOut</a>
      </div>

      <h3 class="text-2xl text-cyan-600 font-serif underline underline-offset-4 decoration-1 decoration-green-500 mt-10">
        Edit Your Post
      </h3>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          class="w-1/3 mx-auto p-3 resize-none block text-center mb-5 outline-none bg-transparent border-2 border-zinc-800 rounded-md mt-5"
          name="content"
        ><%= post.content %></textarea>
        <input
          class="px-3 mx-auto text-black rounded-md block py-2 bg-yellow-500"
          type="submit"
          value="update post"
        />
      </form>
    </div>
  </body>
</html>
